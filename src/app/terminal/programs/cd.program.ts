export function cd(terminal, args) {
  return 'Error: cd has not been implemented yet.';
};

export const cd_help = {
  command: ['cd', '<path>'],
  discription:  [
    'Changes the workspace directory.',
  ],
};