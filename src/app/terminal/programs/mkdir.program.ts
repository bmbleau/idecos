export function mkdir(terminal, args) {
  return 'Error: mkdir has not been implemented yet.';
};

export const mkdir_help = {
  command: ['mkdir', '<path>'],
  discription:  [
    'Makes a directory at the given path.',
  ],
};